// Your JavaScript code here
// For example, you could add interactive elements like:
// - Toggling sections to show/hide details
// - Adding animations or transitions
// - Creating a downloadable PDF version of the resume

// Here's a simple example to toggle a section:

function toggleSection(sectionId) {
    const section = document.getElementById(sectionId);
    section.classList.toggle('hidden');
}